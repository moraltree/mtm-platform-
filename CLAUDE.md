# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is a fresh scaffold. As of now it contains only `README.md` ("# mtm platform") — no source code, build tooling, package manifests, or tests have been added yet.

There are no commands to build, lint, or test because no language, framework, or tooling has been chosen yet.

## Guidance for future sessions

- When source code, a package manifest (e.g. `package.json`, `pyproject.toml`, `go.mod`), or CI config is added to this repo, update this file with the real build/lint/test commands and a description of the architecture.
- Do not assume a particular language or framework — check what's actually present before proposing commands or structure.
