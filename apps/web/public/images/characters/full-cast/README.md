# `full-cast/` — approved full-cast (7–8 character) group images

Three images copied (not moved, not generated — see `cp -np` in git
history) from `~/mtm-assets/story-worlds/zulu-savannah-seven/15-approved/`,
the master library's explicitly promoted "website-use ready" subset (see
that library's `PRODUCTION_MANIFEST.md`: "9 promoted to `15-approved/`
(website-use ready)" after a per-image count/duplicate/scale/identity
audit — not the whole §6 full-cast generation batch, only the
individually-cleared ones).

- `full-cast-01-group-portrait.png` — warm cream/tan studio portrait, all
  8 characters.
- `full-cast-05-sunset-silhouette-warm.png` — warm sunset-toned group
  scene, all 8 characters.
- `full-cast-09-storytime-circle.png` — the cast gathered around an open
  picture book, all 8 characters.

Used by the `/free30` campaign landing page (`components/patterns/
CampaignLanding`) for its hero side imagery and cast section — see
`lib/characterGroups.ts` for the manifest referencing these paths. Master
originals under `~/mtm-assets/` untouched (copy only).

Distinct from `pairs/` and `groups/` (both still empty/gated — see their
own READMEs): those cover the _small_-group (2–4 character) generation
programme, which is still awaiting explicit approval to run. These three
full-cast images are a different, already-generated, already-approved
manifest section (§6) — nothing here required generating anything new.
