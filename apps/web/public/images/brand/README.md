# `brand/` — the Moral Tree symbol itself

`moral-tree-mark.png` — copied (not moved, not generated) from
`~/mtm-assets/story-worlds/zulu-savannah-seven/11-moral-tree/
moral-tree-hero-06-standalone-vertical-crop.png`, one of the six
**approved, already-generated** standalone Moral Tree images (see that
library's `PRODUCTION_MANIFEST.md`, Section 7 — "DONE", all ✅). Distinct
from the still-**deferred** Section 8 "#1 logo lockup" item (a composed
logo file with wordmark/tagline/border baked in) — that one remains
un-generated pending real code/design requirements, per the manifest.
This file is the tree symbol alone, no text baked in, which is exactly
what a code-composed lockup (icon + live HTML wordmark, not a flattened
logo image) needs.

Used by `components/patterns/CampaignLanding` for the `/free30` brand
bar's icon+wordmark lockup (`styles.brandTreeIcon`). Master original
under `~/mtm-assets/` untouched (copy only).
