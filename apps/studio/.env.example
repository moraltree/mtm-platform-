# Copy to .env for local Studio development: cp .env.example .env
# Studio env vars must be prefixed SANITY_STUDIO_ to be exposed to the app.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
